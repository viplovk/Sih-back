{
  "name": "algoriot-backend",
  "version": "1.0.0",
  "description": "ALGORIOT Backend - Hybrid AI-NWP Multi-Model Forecast Blending System for Disaster Management (SIH26081)",
  "scripts": {
    "dev": "uvicorn app.main:app --host 0.0.0.0 --port 3000 --reload",
    "start": "uvicorn app.main:app --host 0.0.0.0 --port 3000",
    "build": "echo 'Build complete'",
    "lint": "pytest -q",
    "test": "pytest"
  }
}
